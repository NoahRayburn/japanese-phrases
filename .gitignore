node_modules
dist
.DS_Store
.env
.env.local

# TypeScript / Vite build artifacts
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Local Claude Code config (not part of the app)
.claude/

# Audio files (public/audio/*.m4a) are committed so the GitHub Pages
# deploy is self-contained. Regenerate with `npm run generate-audio`.
